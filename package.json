{
  "dependencies": {
    "cpy-cli": "^5.0.0",
    "respec": "^35.2.2",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "prebuild": "rimraf -rf dist && mkdir dist && cpy ldt4ssc-logo.jpg dist",
    "build": "respec --src index.html --out dist/index.html",
    "serve": "npm run build && http-server dist"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
